# WorkOS API Configuration
# Get these values from your WorkOS Dashboard: https://dashboard.workos.com/

# API Key: Dashboard → Developer → API Keys
WORKOS_API_KEY=Insert your WorkOS API key here

# Client ID: Dashboard → Configuration → Authentication
WORKOS_CLIENT_ID=Insert your WorkOS Client ID here

# Organization ID: Dashboard → Organizations → [Your Organization] → Copy Organization ID
# This organization must have the Test Provider connection configured
WORKOS_ORGANIZATION_ID=Insert your Organization ID here

# Redirect URI for OAuth callback
# This must match the redirect URI configured in your WorkOS Dashboard
# Dashboard → Configuration → Authentication → Redirect URIs
REDIRECT_URI=http://localhost:8000/auth/callback
